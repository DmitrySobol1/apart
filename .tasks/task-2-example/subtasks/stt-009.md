Title: feature / Dev proxy & config — /api proxy — SPA fallback
Agent: Code Implementer

Goal
- Configure Vite dev proxy for `/api` and ensure SPA fallback works for client-side routing.

Tech Requirements
- Technologies: Vite, TypeScript
- Vite config: proxy `/api` to `VITE_API_BASE_URL` (read from env)
- Router base path considered if served under subpath
- Ensure container/server serves `index.html` as fallback for SPA routes
- Size: ≤ 100 LOC
- Dependencies: stt-004, stt-003

Acceptance Criteria
- `vite.config.*` contains a working proxy for `/api` respecting `VITE_API_BASE_URL`
- SPA fallback behavior is configured (dev and container image)
- Client fetcher uses `/api` prefix without CORS issues in dev

Refs
- .tasks/task-2/subtasks/index.md
- .tasks/task-2/plan.md
- .memory-bank/steering/decompose-conventions.md
- .memory-bank/steering/development-conventions.md
