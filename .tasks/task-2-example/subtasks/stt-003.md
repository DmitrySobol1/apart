Title: feature / App shell & routing — pages wired — SPA routes
Agent: Code Implementer

Goal
- Provide a basic PageShell and route structure for `/`, `/channels/:username`, and `/channels/:username/posts/:messageId`.

Tech Requirements
- Technologies: React, React Router (`react-router-dom`), TypeScript
- Create `PageShell` and three page components with minimal placeholders
- Use `BrowserRouter` (ensure SPA fallback will be configured later)
- Size: ≤ 300 LOC
- Dependencies: stt-001, stt-002

Acceptance Criteria
- Navigating to each route displays a distinct placeholder view
- Shared layout (PageShell) renders around all pages
- No runtime route errors; 404 handled via a simple fallback route

Refs
- .tasks/task-2/subtasks/index.md
- .tasks/task-2/plan.md
- .tasks/task-2/ui-wireframes.md
- .memory-bank/steering/decompose-conventions.md
- .memory-bank/steering/development-conventions.md
