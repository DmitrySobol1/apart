Title: feature / API fetcher — SWR + error mapping — single entry
Agent: Code Implementer

Goal
- Implement a shared `api.ts` fetcher for GET requests with unified JSON parsing and UI-friendly error mapping.

Tech Requirements
- Technologies: TypeScript, SWR, Fetch API
- Prefix requests with `/api` (Vite dev proxy will forward to backend)
- Define a lightweight `ApiError` shape and map HTTP 400/404/5xx to friendly messages
- Export a generic `fetcher<T>(key)` for SWR usage
- Size: ≤ 250 LOC
- Dependencies: stt-001, stt-003

Acceptance Criteria
- `src/lib/api.ts` exports a reusable fetcher and `ApiError` type
- Non-2xx responses produce a parsed `ApiError` when possible; otherwise a default message
- SWR examples in pages compile and use the shared fetcher

Refs
- .tasks/task-2/subtasks/index.md
- .tasks/task-2/plan.md
- .tasks/task-2/api-contracts.md
- .memory-bank/steering/decompose-conventions.md
- .memory-bank/steering/development-conventions.md
